name: suri
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.0
  hooks_riverpod: ^2.5.1
  firebase_auth: ^5.1.0
  flutter_svg: ^2.0.10+1
  google_sign_in: ^6.2.1
  cloud_firestore: ^5.0.1
  awesome_notifications: ^0.9.3+1
  firebase_messaging: ^15.0.1
  googleapis_auth: ^1.6.0
  smooth_page_indicator: ^1.1.0
  firebase_app_check: ^0.3.0+2
  intl: ^0.19.0
  shimmer: ^3.0.0
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets: 
    - assets/icons/
    - lib/services/messaging/ServiceAccountKeyDevelopment.json
    # - lib/services/messaging/ServiceAccountKeyProduction.json

