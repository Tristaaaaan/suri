import 'package:flutter/material.dart';

class ObservePage extends StatelessWidget {
  const ObservePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Observe Page"),
    );
  }
}
